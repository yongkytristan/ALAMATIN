# Web application

The review interface will live here. No frontend stack has been selected in the
repository-skeleton phase; that decision must be recorded before dependencies
or generated application files are added.

The API shape is already frozen independently of the future frontend stack.
Use `address-contract.js` to load the canonical
`contracts/address-api.v1.schema.json`; do not create a frontend-only copy or
rename `model_score` to `confidence`.
