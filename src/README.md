# Source packages

Backend, address-processing, and ML packages live here after their respective
issues pass scope and protocol gates. `alamatin.reference_hierarchy` provides
the validated exact/ambiguous/not-found lookup contract for canonical
administrative and postal reference rows. Keep PII handling isolated from
downstream address processing and preserve provenance for every derived value.
